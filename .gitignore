# Build artifacts
/oriel
/dist/
# web/dist is intentionally committed so `go install` works (go:embed needs it
# present in the module). Rebuild with `make build` and commit it when you change
# the frontend, CI fails if it's stale (see .github/workflows/ci.yml).

# Dependencies
/web/node_modules/

# Editor / OS
.DS_Store
*.log
/oriel-*

/research/
